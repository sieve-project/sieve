# This file is automatically generated by gen_github_action.py on 2022-04-18 21:25:48.573036
name: Sieve Bug Reproduction
'on':
  workflow_dispatch: null
env:
  IMAGE_NAMESPACE: ghcr.io/sieve-project/action
jobs:
  cass-operator:
    runs-on: self-hosted
    env:
      GOPATH: /home/runner/go
      KUBECONFIG: /home/runner/.kube/config
    steps:
    - uses: actions/checkout@v2
    - name: Setup Git
      run: 'git config --global user.name "sieve"

        git config --global user.email "sieve@sieve.com"'
    - name: Setup Go environment
      uses: actions/setup-go@v2.1.3
      with:
        go-version: 1.15
    - name: Setup Python
      uses: actions/setup-python@v2.2.2
      with:
        python-version: 3.7
    - name: Setup GitHub Package Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
    - name: Install Python Packages
      run: pip install -r requirements.txt
    - name: Install Kind
      run: 'go get sigs.k8s.io/kind

        kind'
    - name: Install Mage
      run: 'go get -u github.com/magefile/mage

        mage -h'
    - name: Install Helm
      run: 'wget https://get.helm.sh/helm-v3.6.0-linux-amd64.tar.gz

        tar -zxvf helm-v3.6.0-linux-amd64.tar.gz

        sudo mv linux-amd64/helm /usr/local/bin/helm

        helm'
    - name: Sieve CI config generate
      run: 'echo "{\"workload_hard_timeout\": 1000}" > sieve_config.json

        cat sieve_config.json'
    - name: Sieve Test - cass-operator intermediate-state-1
      run: python3 reproduce_bugs.py -p cass-operator -b intermediate-state-1 -d $IMAGE_NAMESPACE
    - name: Sieve Test - cass-operator stale-state-1
      run: python3 reproduce_bugs.py -p cass-operator -b stale-state-1 -d $IMAGE_NAMESPACE
    - uses: actions/upload-artifact@v2
      if: always()
      with:
        name: sieve-cass-operator-log
        path: log
    - name: Remove cluster
      if: always()
      run: kind delete cluster
    - uses: JamesIves/github-pages-deploy-action@4.1.5
      name: Persistent oracle data
      with:
        branch: oracle-data
        folder: examples/cass-operator/oracle
        target-folder: cass-operator/oracle
    - name: Clean images
      if: always()
      run: docker image prune -a -f && docker builder prune -a -f && docker system df
  cassandra-operator:
    runs-on: self-hosted
    env:
      GOPATH: /home/runner/go
      KUBECONFIG: /home/runner/.kube/config
    steps:
    - uses: actions/checkout@v2
    - name: Setup Git
      run: 'git config --global user.name "sieve"

        git config --global user.email "sieve@sieve.com"'
    - name: Setup Go environment
      uses: actions/setup-go@v2.1.3
      with:
        go-version: 1.15
    - name: Setup Python
      uses: actions/setup-python@v2.2.2
      with:
        python-version: 3.7
    - name: Setup GitHub Package Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
    - name: Install Python Packages
      run: pip install -r requirements.txt
    - name: Install Kind
      run: 'go get sigs.k8s.io/kind

        kind'
    - name: Install Mage
      run: 'go get -u github.com/magefile/mage

        mage -h'
    - name: Install Helm
      run: 'wget https://get.helm.sh/helm-v3.6.0-linux-amd64.tar.gz

        tar -zxvf helm-v3.6.0-linux-amd64.tar.gz

        sudo mv linux-amd64/helm /usr/local/bin/helm

        helm'
    - name: Sieve CI config generate
      run: 'echo "{\"workload_hard_timeout\": 1000}" > sieve_config.json

        cat sieve_config.json'
    - name: Sieve Test - cassandra-operator stale-state-1
      run: python3 reproduce_bugs.py -p cassandra-operator -b stale-state-1 -d $IMAGE_NAMESPACE
    - name: Sieve Test - cassandra-operator stale-state-2
      run: python3 reproduce_bugs.py -p cassandra-operator -b stale-state-2 -d $IMAGE_NAMESPACE
    - name: Sieve Test - cassandra-operator unobserved-state-1
      run: python3 reproduce_bugs.py -p cassandra-operator -b unobserved-state-1 -d $IMAGE_NAMESPACE
    - uses: actions/upload-artifact@v2
      if: always()
      with:
        name: sieve-cassandra-operator-log
        path: log
    - name: Remove cluster
      if: always()
      run: kind delete cluster
    - uses: JamesIves/github-pages-deploy-action@4.1.5
      name: Persistent oracle data
      with:
        branch: oracle-data
        folder: examples/cassandra-operator/oracle
        target-folder: cassandra-operator/oracle
    - name: Clean images
      if: always()
      run: docker image prune -a -f && docker builder prune -a -f && docker system df
  casskop-operator:
    runs-on: self-hosted
    env:
      GOPATH: /home/runner/go
      KUBECONFIG: /home/runner/.kube/config
    steps:
    - uses: actions/checkout@v2
    - name: Setup Git
      run: 'git config --global user.name "sieve"

        git config --global user.email "sieve@sieve.com"'
    - name: Setup Go environment
      uses: actions/setup-go@v2.1.3
      with:
        go-version: 1.15
    - name: Setup Python
      uses: actions/setup-python@v2.2.2
      with:
        python-version: 3.7
    - name: Setup GitHub Package Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
    - name: Install Python Packages
      run: pip install -r requirements.txt
    - name: Install Kind
      run: 'go get sigs.k8s.io/kind

        kind'
    - name: Install Mage
      run: 'go get -u github.com/magefile/mage

        mage -h'
    - name: Install Helm
      run: 'wget https://get.helm.sh/helm-v3.6.0-linux-amd64.tar.gz

        tar -zxvf helm-v3.6.0-linux-amd64.tar.gz

        sudo mv linux-amd64/helm /usr/local/bin/helm

        helm'
    - name: Sieve CI config generate
      run: 'echo "{\"workload_hard_timeout\": 1000}" > sieve_config.json

        cat sieve_config.json'
    - name: Sieve Test - casskop-operator intermediate-state-1
      run: python3 reproduce_bugs.py -p casskop-operator -b intermediate-state-1 -d $IMAGE_NAMESPACE
    - name: Sieve Test - casskop-operator stale-state-1
      run: python3 reproduce_bugs.py -p casskop-operator -b stale-state-1 -d $IMAGE_NAMESPACE
    - name: Sieve Test - casskop-operator stale-state-2
      run: python3 reproduce_bugs.py -p casskop-operator -b stale-state-2 -d $IMAGE_NAMESPACE
    - name: Sieve Test - casskop-operator unobserved-state-1
      run: python3 reproduce_bugs.py -p casskop-operator -b unobserved-state-1 -d $IMAGE_NAMESPACE
    - uses: actions/upload-artifact@v2
      if: always()
      with:
        name: sieve-casskop-operator-log
        path: log
    - name: Remove cluster
      if: always()
      run: kind delete cluster
    - uses: JamesIves/github-pages-deploy-action@4.1.5
      name: Persistent oracle data
      with:
        branch: oracle-data
        folder: examples/casskop-operator/oracle
        target-folder: casskop-operator/oracle
    - name: Clean images
      if: always()
      run: docker image prune -a -f && docker builder prune -a -f && docker system df
  elastic-operator:
    runs-on: self-hosted
    env:
      GOPATH: /home/runner/go
      KUBECONFIG: /home/runner/.kube/config
    steps:
    - uses: actions/checkout@v2
    - name: Setup Git
      run: 'git config --global user.name "sieve"

        git config --global user.email "sieve@sieve.com"'
    - name: Setup Go environment
      uses: actions/setup-go@v2.1.3
      with:
        go-version: 1.15
    - name: Setup Python
      uses: actions/setup-python@v2.2.2
      with:
        python-version: 3.7
    - name: Setup GitHub Package Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
    - name: Install Python Packages
      run: pip install -r requirements.txt
    - name: Install Kind
      run: 'go get sigs.k8s.io/kind

        kind'
    - name: Install Mage
      run: 'go get -u github.com/magefile/mage

        mage -h'
    - name: Install Helm
      run: 'wget https://get.helm.sh/helm-v3.6.0-linux-amd64.tar.gz

        tar -zxvf helm-v3.6.0-linux-amd64.tar.gz

        sudo mv linux-amd64/helm /usr/local/bin/helm

        helm'
    - name: Sieve CI config generate
      run: 'echo "{\"workload_hard_timeout\": 1000}" > sieve_config.json

        cat sieve_config.json'
    - name: Sieve Test - elastic-operator stale-state-1
      run: python3 reproduce_bugs.py -p elastic-operator -b stale-state-1 -d $IMAGE_NAMESPACE
    - name: Sieve Test - elastic-operator stale-state-2
      run: python3 reproduce_bugs.py -p elastic-operator -b stale-state-2 -d $IMAGE_NAMESPACE
    - uses: actions/upload-artifact@v2
      if: always()
      with:
        name: sieve-elastic-operator-log
        path: log
    - name: Remove cluster
      if: always()
      run: kind delete cluster
    - uses: JamesIves/github-pages-deploy-action@4.1.5
      name: Persistent oracle data
      with:
        branch: oracle-data
        folder: examples/elastic-operator/oracle
        target-folder: elastic-operator/oracle
    - name: Clean images
      if: always()
      run: docker image prune -a -f && docker builder prune -a -f && docker system df
  mongodb-operator:
    runs-on: self-hosted
    env:
      GOPATH: /home/runner/go
      KUBECONFIG: /home/runner/.kube/config
    steps:
    - uses: actions/checkout@v2
    - name: Setup Git
      run: 'git config --global user.name "sieve"

        git config --global user.email "sieve@sieve.com"'
    - name: Setup Go environment
      uses: actions/setup-go@v2.1.3
      with:
        go-version: 1.15
    - name: Setup Python
      uses: actions/setup-python@v2.2.2
      with:
        python-version: 3.7
    - name: Setup GitHub Package Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
    - name: Install Python Packages
      run: pip install -r requirements.txt
    - name: Install Kind
      run: 'go get sigs.k8s.io/kind

        kind'
    - name: Install Mage
      run: 'go get -u github.com/magefile/mage

        mage -h'
    - name: Install Helm
      run: 'wget https://get.helm.sh/helm-v3.6.0-linux-amd64.tar.gz

        tar -zxvf helm-v3.6.0-linux-amd64.tar.gz

        sudo mv linux-amd64/helm /usr/local/bin/helm

        helm'
    - name: Sieve CI config generate
      run: 'echo "{\"workload_hard_timeout\": 1000}" > sieve_config.json

        cat sieve_config.json'
    - name: Sieve Test - mongodb-operator intermediate-state-1
      run: python3 reproduce_bugs.py -p mongodb-operator -b intermediate-state-1 -d $IMAGE_NAMESPACE
    - name: Sieve Test - mongodb-operator intermediate-state-2
      run: python3 reproduce_bugs.py -p mongodb-operator -b intermediate-state-2 -d $IMAGE_NAMESPACE
    - name: Sieve Test - mongodb-operator stale-state-1
      run: python3 reproduce_bugs.py -p mongodb-operator -b stale-state-1 -d $IMAGE_NAMESPACE
    - name: Sieve Test - mongodb-operator stale-state-2
      run: python3 reproduce_bugs.py -p mongodb-operator -b stale-state-2 -d $IMAGE_NAMESPACE
    - name: Sieve Test - mongodb-operator stale-state-3
      run: python3 reproduce_bugs.py -p mongodb-operator -b stale-state-3 -d $IMAGE_NAMESPACE
    - name: Sieve Test - mongodb-operator unobserved-state-1
      run: python3 reproduce_bugs.py -p mongodb-operator -b unobserved-state-1 -d $IMAGE_NAMESPACE
    - uses: actions/upload-artifact@v2
      if: always()
      with:
        name: sieve-mongodb-operator-log
        path: log
    - name: Remove cluster
      if: always()
      run: kind delete cluster
    - uses: JamesIves/github-pages-deploy-action@4.1.5
      name: Persistent oracle data
      with:
        branch: oracle-data
        folder: examples/mongodb-operator/oracle
        target-folder: mongodb-operator/oracle
    - name: Clean images
      if: always()
      run: docker image prune -a -f && docker builder prune -a -f && docker system df
  nifikop-operator:
    runs-on: self-hosted
    env:
      GOPATH: /home/runner/go
      KUBECONFIG: /home/runner/.kube/config
    steps:
    - uses: actions/checkout@v2
    - name: Setup Git
      run: 'git config --global user.name "sieve"

        git config --global user.email "sieve@sieve.com"'
    - name: Setup Go environment
      uses: actions/setup-go@v2.1.3
      with:
        go-version: 1.15
    - name: Setup Python
      uses: actions/setup-python@v2.2.2
      with:
        python-version: 3.7
    - name: Setup GitHub Package Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
    - name: Install Python Packages
      run: pip install -r requirements.txt
    - name: Install Kind
      run: 'go get sigs.k8s.io/kind

        kind'
    - name: Install Mage
      run: 'go get -u github.com/magefile/mage

        mage -h'
    - name: Install Helm
      run: 'wget https://get.helm.sh/helm-v3.6.0-linux-amd64.tar.gz

        tar -zxvf helm-v3.6.0-linux-amd64.tar.gz

        sudo mv linux-amd64/helm /usr/local/bin/helm

        helm'
    - name: Sieve CI config generate
      run: 'echo "{\"workload_hard_timeout\": 1000}" > sieve_config.json

        cat sieve_config.json'
    - name: Sieve Test - nifikop-operator intermediate-state-1
      run: python3 reproduce_bugs.py -p nifikop-operator -b intermediate-state-1 -d $IMAGE_NAMESPACE
    - uses: actions/upload-artifact@v2
      if: always()
      with:
        name: sieve-nifikop-operator-log
        path: log
    - name: Remove cluster
      if: always()
      run: kind delete cluster
    - uses: JamesIves/github-pages-deploy-action@4.1.5
      name: Persistent oracle data
      with:
        branch: oracle-data
        folder: examples/nifikop-operator/oracle
        target-folder: nifikop-operator/oracle
    - name: Clean images
      if: always()
      run: docker image prune -a -f && docker builder prune -a -f && docker system df
  rabbitmq-operator:
    runs-on: self-hosted
    env:
      GOPATH: /home/runner/go
      KUBECONFIG: /home/runner/.kube/config
    steps:
    - uses: actions/checkout@v2
    - name: Setup Git
      run: 'git config --global user.name "sieve"

        git config --global user.email "sieve@sieve.com"'
    - name: Setup Go environment
      uses: actions/setup-go@v2.1.3
      with:
        go-version: 1.15
    - name: Setup Python
      uses: actions/setup-python@v2.2.2
      with:
        python-version: 3.7
    - name: Setup GitHub Package Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
    - name: Install Python Packages
      run: pip install -r requirements.txt
    - name: Install Kind
      run: 'go get sigs.k8s.io/kind

        kind'
    - name: Install Mage
      run: 'go get -u github.com/magefile/mage

        mage -h'
    - name: Install Helm
      run: 'wget https://get.helm.sh/helm-v3.6.0-linux-amd64.tar.gz

        tar -zxvf helm-v3.6.0-linux-amd64.tar.gz

        sudo mv linux-amd64/helm /usr/local/bin/helm

        helm'
    - name: Sieve CI config generate
      run: 'echo "{\"workload_hard_timeout\": 1000}" > sieve_config.json

        cat sieve_config.json'
    - name: Sieve Test - rabbitmq-operator intermediate-state-1
      run: python3 reproduce_bugs.py -p rabbitmq-operator -b intermediate-state-1 -d $IMAGE_NAMESPACE
    - name: Sieve Test - rabbitmq-operator stale-state-1
      run: python3 reproduce_bugs.py -p rabbitmq-operator -b stale-state-1 -d $IMAGE_NAMESPACE
    - name: Sieve Test - rabbitmq-operator stale-state-2
      run: python3 reproduce_bugs.py -p rabbitmq-operator -b stale-state-2 -d $IMAGE_NAMESPACE
    - name: Sieve Test - rabbitmq-operator unobserved-state-1
      run: python3 reproduce_bugs.py -p rabbitmq-operator -b unobserved-state-1 -d $IMAGE_NAMESPACE
    - uses: actions/upload-artifact@v2
      if: always()
      with:
        name: sieve-rabbitmq-operator-log
        path: log
    - name: Remove cluster
      if: always()
      run: kind delete cluster
    - uses: JamesIves/github-pages-deploy-action@4.1.5
      name: Persistent oracle data
      with:
        branch: oracle-data
        folder: examples/rabbitmq-operator/oracle
        target-folder: rabbitmq-operator/oracle
    - name: Clean images
      if: always()
      run: docker image prune -a -f && docker builder prune -a -f && docker system df
  xtradb-operator:
    runs-on: self-hosted
    env:
      GOPATH: /home/runner/go
      KUBECONFIG: /home/runner/.kube/config
    steps:
    - uses: actions/checkout@v2
    - name: Setup Git
      run: 'git config --global user.name "sieve"

        git config --global user.email "sieve@sieve.com"'
    - name: Setup Go environment
      uses: actions/setup-go@v2.1.3
      with:
        go-version: 1.15
    - name: Setup Python
      uses: actions/setup-python@v2.2.2
      with:
        python-version: 3.7
    - name: Setup GitHub Package Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
    - name: Install Python Packages
      run: pip install -r requirements.txt
    - name: Install Kind
      run: 'go get sigs.k8s.io/kind

        kind'
    - name: Install Mage
      run: 'go get -u github.com/magefile/mage

        mage -h'
    - name: Install Helm
      run: 'wget https://get.helm.sh/helm-v3.6.0-linux-amd64.tar.gz

        tar -zxvf helm-v3.6.0-linux-amd64.tar.gz

        sudo mv linux-amd64/helm /usr/local/bin/helm

        helm'
    - name: Sieve CI config generate
      run: 'echo "{\"workload_hard_timeout\": 1000}" > sieve_config.json

        cat sieve_config.json'
    - name: Sieve Test - xtradb-operator intermediate-state-1
      run: python3 reproduce_bugs.py -p xtradb-operator -b intermediate-state-1 -d $IMAGE_NAMESPACE
    - name: Sieve Test - xtradb-operator intermediate-state-2
      run: python3 reproduce_bugs.py -p xtradb-operator -b intermediate-state-2 -d $IMAGE_NAMESPACE
    - name: Sieve Test - xtradb-operator stale-state-1
      run: python3 reproduce_bugs.py -p xtradb-operator -b stale-state-1 -d $IMAGE_NAMESPACE
    - name: Sieve Test - xtradb-operator stale-state-2
      run: python3 reproduce_bugs.py -p xtradb-operator -b stale-state-2 -d $IMAGE_NAMESPACE
    - name: Sieve Test - xtradb-operator stale-state-3
      run: python3 reproduce_bugs.py -p xtradb-operator -b stale-state-3 -d $IMAGE_NAMESPACE
    - name: Sieve Test - xtradb-operator unobserved-state-1
      run: python3 reproduce_bugs.py -p xtradb-operator -b unobserved-state-1 -d $IMAGE_NAMESPACE
    - uses: actions/upload-artifact@v2
      if: always()
      with:
        name: sieve-xtradb-operator-log
        path: log
    - name: Remove cluster
      if: always()
      run: kind delete cluster
    - uses: JamesIves/github-pages-deploy-action@4.1.5
      name: Persistent oracle data
      with:
        branch: oracle-data
        folder: examples/xtradb-operator/oracle
        target-folder: xtradb-operator/oracle
    - name: Clean images
      if: always()
      run: docker image prune -a -f && docker builder prune -a -f && docker system df
  yugabyte-operator:
    runs-on: self-hosted
    env:
      GOPATH: /home/runner/go
      KUBECONFIG: /home/runner/.kube/config
    steps:
    - uses: actions/checkout@v2
    - name: Setup Git
      run: 'git config --global user.name "sieve"

        git config --global user.email "sieve@sieve.com"'
    - name: Setup Go environment
      uses: actions/setup-go@v2.1.3
      with:
        go-version: 1.15
    - name: Setup Python
      uses: actions/setup-python@v2.2.2
      with:
        python-version: 3.7
    - name: Setup GitHub Package Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
    - name: Install Python Packages
      run: pip install -r requirements.txt
    - name: Install Kind
      run: 'go get sigs.k8s.io/kind

        kind'
    - name: Install Mage
      run: 'go get -u github.com/magefile/mage

        mage -h'
    - name: Install Helm
      run: 'wget https://get.helm.sh/helm-v3.6.0-linux-amd64.tar.gz

        tar -zxvf helm-v3.6.0-linux-amd64.tar.gz

        sudo mv linux-amd64/helm /usr/local/bin/helm

        helm'
    - name: Sieve CI config generate
      run: 'echo "{\"workload_hard_timeout\": 1000}" > sieve_config.json

        cat sieve_config.json'
    - name: Sieve Test - yugabyte-operator stale-state-1
      run: python3 reproduce_bugs.py -p yugabyte-operator -b stale-state-1 -d $IMAGE_NAMESPACE
    - name: Sieve Test - yugabyte-operator stale-state-2
      run: python3 reproduce_bugs.py -p yugabyte-operator -b stale-state-2 -d $IMAGE_NAMESPACE
    - name: Sieve Test - yugabyte-operator unobserved-state-1
      run: python3 reproduce_bugs.py -p yugabyte-operator -b unobserved-state-1 -d $IMAGE_NAMESPACE
    - uses: actions/upload-artifact@v2
      if: always()
      with:
        name: sieve-yugabyte-operator-log
        path: log
    - name: Remove cluster
      if: always()
      run: kind delete cluster
    - uses: JamesIves/github-pages-deploy-action@4.1.5
      name: Persistent oracle data
      with:
        branch: oracle-data
        folder: examples/yugabyte-operator/oracle
        target-folder: yugabyte-operator/oracle
    - name: Clean images
      if: always()
      run: docker image prune -a -f && docker builder prune -a -f && docker system df
  zookeeper-operator:
    runs-on: self-hosted
    env:
      GOPATH: /home/runner/go
      KUBECONFIG: /home/runner/.kube/config
    steps:
    - uses: actions/checkout@v2
    - name: Setup Git
      run: 'git config --global user.name "sieve"

        git config --global user.email "sieve@sieve.com"'
    - name: Setup Go environment
      uses: actions/setup-go@v2.1.3
      with:
        go-version: 1.15
    - name: Setup Python
      uses: actions/setup-python@v2.2.2
      with:
        python-version: 3.7
    - name: Setup GitHub Package Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
    - name: Install Python Packages
      run: pip install -r requirements.txt
    - name: Install Kind
      run: 'go get sigs.k8s.io/kind

        kind'
    - name: Install Mage
      run: 'go get -u github.com/magefile/mage

        mage -h'
    - name: Install Helm
      run: 'wget https://get.helm.sh/helm-v3.6.0-linux-amd64.tar.gz

        tar -zxvf helm-v3.6.0-linux-amd64.tar.gz

        sudo mv linux-amd64/helm /usr/local/bin/helm

        helm'
    - name: Sieve CI config generate
      run: 'echo "{\"workload_hard_timeout\": 1000}" > sieve_config.json

        cat sieve_config.json'
    - name: Sieve Test - zookeeper-operator stale-state-1
      run: python3 reproduce_bugs.py -p zookeeper-operator -b stale-state-1 -d $IMAGE_NAMESPACE
    - name: Sieve Test - zookeeper-operator stale-state-2
      run: python3 reproduce_bugs.py -p zookeeper-operator -b stale-state-2 -d $IMAGE_NAMESPACE
    - uses: actions/upload-artifact@v2
      if: always()
      with:
        name: sieve-zookeeper-operator-log
        path: log
    - name: Remove cluster
      if: always()
      run: kind delete cluster
    - uses: JamesIves/github-pages-deploy-action@4.1.5
      name: Persistent oracle data
      with:
        branch: oracle-data
        folder: examples/zookeeper-operator/oracle
        target-folder: zookeeper-operator/oracle
    - name: Clean images
      if: always()
      run: docker image prune -a -f && docker builder prune -a -f && docker system df
