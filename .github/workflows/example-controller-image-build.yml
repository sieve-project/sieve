# This file is automatically generated by gen_github_action.py on 2023-03-05 04:51:10.008737
name: Example Controller Image Build
'on':
  workflow_dispatch: null
env:
  IMAGE_NAMESPACE: ghcr.io/sieve-project/action
jobs:
  cassandra-operator:
    runs-on: ubuntu-latest
    env:
      GOPATH: /home/runner/go
      KUBECONFIG: /home/runner/.kube/config
    steps:
    - uses: actions/checkout@v2
    - name: Setup Git
      run: 'git config --global user.name "sieve"

        git config --global user.email "sieve@sieve.com"'
    - name: Setup Go environment
      uses: actions/setup-go@v2.1.3
      with:
        go-version: 1.19
    - name: Setup Python
      uses: actions/setup-python@v2.2.2
      with:
        python-version: 3.7
    - name: Setup GitHub Package Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
    - name: Install Python Packages
      run: pip install -r requirements.txt
    - name: Install Kind
      run: 'GO111MODULE="on" go install sigs.k8s.io/kind@v0.13.0

        kind'
    - name: Install Helm
      run: 'wget https://get.helm.sh/helm-v3.6.0-linux-amd64.tar.gz

        tar -zxvf helm-v3.6.0-linux-amd64.tar.gz

        sudo mv linux-amd64/helm /usr/local/bin/helm

        helm'
    - name: Sieve CI config generate
      run: 'echo "{\"workload_conditional_wait_timeout\": 1000}" > sieve_config.json

        cat sieve_config.json'
    - name: Build Image - learn
      run: 'python3 build.py -c examples/cassandra-operator/ -m learn -p -r $IMAGE_NAMESPACE '
    - name: Build Image - test
      run: 'python3 build.py -c examples/cassandra-operator/ -m test -p -r $IMAGE_NAMESPACE '
    - name: Build Image - vanilla
      run: 'python3 build.py -c examples/cassandra-operator/ -m vanilla -p -r $IMAGE_NAMESPACE '
    - name: Clean images
      if: always()
      run: docker image prune -a -f && docker builder prune -a -f && docker system df
  casskop-operator:
    runs-on: ubuntu-latest
    env:
      GOPATH: /home/runner/go
      KUBECONFIG: /home/runner/.kube/config
    steps:
    - uses: actions/checkout@v2
    - name: Setup Git
      run: 'git config --global user.name "sieve"

        git config --global user.email "sieve@sieve.com"'
    - name: Setup Go environment
      uses: actions/setup-go@v2.1.3
      with:
        go-version: 1.19
    - name: Setup Python
      uses: actions/setup-python@v2.2.2
      with:
        python-version: 3.7
    - name: Setup GitHub Package Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
    - name: Install Python Packages
      run: pip install -r requirements.txt
    - name: Install Kind
      run: 'GO111MODULE="on" go install sigs.k8s.io/kind@v0.13.0

        kind'
    - name: Install Helm
      run: 'wget https://get.helm.sh/helm-v3.6.0-linux-amd64.tar.gz

        tar -zxvf helm-v3.6.0-linux-amd64.tar.gz

        sudo mv linux-amd64/helm /usr/local/bin/helm

        helm'
    - name: Sieve CI config generate
      run: 'echo "{\"workload_conditional_wait_timeout\": 1000}" > sieve_config.json

        cat sieve_config.json'
    - name: Build Image - learn
      run: 'python3 build.py -c examples/casskop-operator/ -m learn -p -r $IMAGE_NAMESPACE '
    - name: Build Image - test
      run: 'python3 build.py -c examples/casskop-operator/ -m test -p -r $IMAGE_NAMESPACE '
    - name: Build Image - vanilla
      run: 'python3 build.py -c examples/casskop-operator/ -m vanilla -p -r $IMAGE_NAMESPACE '
    - name: Clean images
      if: always()
      run: docker image prune -a -f && docker builder prune -a -f && docker system df
  mongodb-operator:
    runs-on: ubuntu-latest
    env:
      GOPATH: /home/runner/go
      KUBECONFIG: /home/runner/.kube/config
    steps:
    - uses: actions/checkout@v2
    - name: Setup Git
      run: 'git config --global user.name "sieve"

        git config --global user.email "sieve@sieve.com"'
    - name: Setup Go environment
      uses: actions/setup-go@v2.1.3
      with:
        go-version: 1.19
    - name: Setup Python
      uses: actions/setup-python@v2.2.2
      with:
        python-version: 3.7
    - name: Setup GitHub Package Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
    - name: Install Python Packages
      run: pip install -r requirements.txt
    - name: Install Kind
      run: 'GO111MODULE="on" go install sigs.k8s.io/kind@v0.13.0

        kind'
    - name: Install Helm
      run: 'wget https://get.helm.sh/helm-v3.6.0-linux-amd64.tar.gz

        tar -zxvf helm-v3.6.0-linux-amd64.tar.gz

        sudo mv linux-amd64/helm /usr/local/bin/helm

        helm'
    - name: Sieve CI config generate
      run: 'echo "{\"workload_conditional_wait_timeout\": 1000}" > sieve_config.json

        cat sieve_config.json'
    - name: Build Image - learn
      run: 'python3 build.py -c examples/mongodb-operator/ -m learn -p -r $IMAGE_NAMESPACE '
    - name: Build Image - test
      run: 'python3 build.py -c examples/mongodb-operator/ -m test -p -r $IMAGE_NAMESPACE '
    - name: Build Image - vanilla
      run: 'python3 build.py -c examples/mongodb-operator/ -m vanilla -p -r $IMAGE_NAMESPACE '
    - name: Clean images
      if: always()
      run: docker image prune -a -f && docker builder prune -a -f && docker system df
  nifikop-operator:
    runs-on: ubuntu-latest
    env:
      GOPATH: /home/runner/go
      KUBECONFIG: /home/runner/.kube/config
    steps:
    - uses: actions/checkout@v2
    - name: Setup Git
      run: 'git config --global user.name "sieve"

        git config --global user.email "sieve@sieve.com"'
    - name: Setup Go environment
      uses: actions/setup-go@v2.1.3
      with:
        go-version: 1.19
    - name: Setup Python
      uses: actions/setup-python@v2.2.2
      with:
        python-version: 3.7
    - name: Setup GitHub Package Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
    - name: Install Python Packages
      run: pip install -r requirements.txt
    - name: Install Kind
      run: 'GO111MODULE="on" go install sigs.k8s.io/kind@v0.13.0

        kind'
    - name: Install Helm
      run: 'wget https://get.helm.sh/helm-v3.6.0-linux-amd64.tar.gz

        tar -zxvf helm-v3.6.0-linux-amd64.tar.gz

        sudo mv linux-amd64/helm /usr/local/bin/helm

        helm'
    - name: Sieve CI config generate
      run: 'echo "{\"workload_conditional_wait_timeout\": 1000}" > sieve_config.json

        cat sieve_config.json'
    - name: Build Image - learn
      run: 'python3 build.py -c examples/nifikop-operator/ -m learn -p -r $IMAGE_NAMESPACE '
    - name: Build Image - test
      run: 'python3 build.py -c examples/nifikop-operator/ -m test -p -r $IMAGE_NAMESPACE '
    - name: Build Image - vanilla
      run: 'python3 build.py -c examples/nifikop-operator/ -m vanilla -p -r $IMAGE_NAMESPACE '
    - name: Clean images
      if: always()
      run: docker image prune -a -f && docker builder prune -a -f && docker system df
  rabbitmq-operator:
    runs-on: ubuntu-latest
    env:
      GOPATH: /home/runner/go
      KUBECONFIG: /home/runner/.kube/config
    steps:
    - uses: actions/checkout@v2
    - name: Setup Git
      run: 'git config --global user.name "sieve"

        git config --global user.email "sieve@sieve.com"'
    - name: Setup Go environment
      uses: actions/setup-go@v2.1.3
      with:
        go-version: 1.19
    - name: Setup Python
      uses: actions/setup-python@v2.2.2
      with:
        python-version: 3.7
    - name: Setup GitHub Package Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
    - name: Install Python Packages
      run: pip install -r requirements.txt
    - name: Install Kind
      run: 'GO111MODULE="on" go install sigs.k8s.io/kind@v0.13.0

        kind'
    - name: Install Helm
      run: 'wget https://get.helm.sh/helm-v3.6.0-linux-amd64.tar.gz

        tar -zxvf helm-v3.6.0-linux-amd64.tar.gz

        sudo mv linux-amd64/helm /usr/local/bin/helm

        helm'
    - name: Sieve CI config generate
      run: 'echo "{\"workload_conditional_wait_timeout\": 1000}" > sieve_config.json

        cat sieve_config.json'
    - name: Build Image - learn
      run: 'python3 build.py -c examples/rabbitmq-operator/ -m learn -p -r $IMAGE_NAMESPACE '
    - name: Build Image - test
      run: 'python3 build.py -c examples/rabbitmq-operator/ -m test -p -r $IMAGE_NAMESPACE '
    - name: Build Image - vanilla
      run: 'python3 build.py -c examples/rabbitmq-operator/ -m vanilla -p -r $IMAGE_NAMESPACE '
    - name: Clean images
      if: always()
      run: docker image prune -a -f && docker builder prune -a -f && docker system df
  xtradb-operator:
    runs-on: ubuntu-latest
    env:
      GOPATH: /home/runner/go
      KUBECONFIG: /home/runner/.kube/config
    steps:
    - uses: actions/checkout@v2
    - name: Setup Git
      run: 'git config --global user.name "sieve"

        git config --global user.email "sieve@sieve.com"'
    - name: Setup Go environment
      uses: actions/setup-go@v2.1.3
      with:
        go-version: 1.19
    - name: Setup Python
      uses: actions/setup-python@v2.2.2
      with:
        python-version: 3.7
    - name: Setup GitHub Package Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
    - name: Install Python Packages
      run: pip install -r requirements.txt
    - name: Install Kind
      run: 'GO111MODULE="on" go install sigs.k8s.io/kind@v0.13.0

        kind'
    - name: Install Helm
      run: 'wget https://get.helm.sh/helm-v3.6.0-linux-amd64.tar.gz

        tar -zxvf helm-v3.6.0-linux-amd64.tar.gz

        sudo mv linux-amd64/helm /usr/local/bin/helm

        helm'
    - name: Sieve CI config generate
      run: 'echo "{\"workload_conditional_wait_timeout\": 1000}" > sieve_config.json

        cat sieve_config.json'
    - name: Build Image - learn
      run: 'python3 build.py -c examples/xtradb-operator/ -m learn -p -r $IMAGE_NAMESPACE '
    - name: Build Image - test
      run: 'python3 build.py -c examples/xtradb-operator/ -m test -p -r $IMAGE_NAMESPACE '
    - name: Build Image - vanilla
      run: 'python3 build.py -c examples/xtradb-operator/ -m vanilla -p -r $IMAGE_NAMESPACE '
    - name: Clean images
      if: always()
      run: docker image prune -a -f && docker builder prune -a -f && docker system df
  yugabyte-operator:
    runs-on: ubuntu-latest
    env:
      GOPATH: /home/runner/go
      KUBECONFIG: /home/runner/.kube/config
    steps:
    - uses: actions/checkout@v2
    - name: Setup Git
      run: 'git config --global user.name "sieve"

        git config --global user.email "sieve@sieve.com"'
    - name: Setup Go environment
      uses: actions/setup-go@v2.1.3
      with:
        go-version: 1.19
    - name: Setup Python
      uses: actions/setup-python@v2.2.2
      with:
        python-version: 3.7
    - name: Setup GitHub Package Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
    - name: Install Python Packages
      run: pip install -r requirements.txt
    - name: Install Kind
      run: 'GO111MODULE="on" go install sigs.k8s.io/kind@v0.13.0

        kind'
    - name: Install Helm
      run: 'wget https://get.helm.sh/helm-v3.6.0-linux-amd64.tar.gz

        tar -zxvf helm-v3.6.0-linux-amd64.tar.gz

        sudo mv linux-amd64/helm /usr/local/bin/helm

        helm'
    - name: Sieve CI config generate
      run: 'echo "{\"workload_conditional_wait_timeout\": 1000}" > sieve_config.json

        cat sieve_config.json'
    - name: Build Image - learn
      run: 'python3 build.py -c examples/yugabyte-operator/ -m learn -p -r $IMAGE_NAMESPACE '
    - name: Build Image - test
      run: 'python3 build.py -c examples/yugabyte-operator/ -m test -p -r $IMAGE_NAMESPACE '
    - name: Build Image - vanilla
      run: 'python3 build.py -c examples/yugabyte-operator/ -m vanilla -p -r $IMAGE_NAMESPACE '
    - name: Clean images
      if: always()
      run: docker image prune -a -f && docker builder prune -a -f && docker system df
  zookeeper-operator:
    runs-on: ubuntu-latest
    env:
      GOPATH: /home/runner/go
      KUBECONFIG: /home/runner/.kube/config
    steps:
    - uses: actions/checkout@v2
    - name: Setup Git
      run: 'git config --global user.name "sieve"

        git config --global user.email "sieve@sieve.com"'
    - name: Setup Go environment
      uses: actions/setup-go@v2.1.3
      with:
        go-version: 1.19
    - name: Setup Python
      uses: actions/setup-python@v2.2.2
      with:
        python-version: 3.7
    - name: Setup GitHub Package Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
    - name: Install Python Packages
      run: pip install -r requirements.txt
    - name: Install Kind
      run: 'GO111MODULE="on" go install sigs.k8s.io/kind@v0.13.0

        kind'
    - name: Install Helm
      run: 'wget https://get.helm.sh/helm-v3.6.0-linux-amd64.tar.gz

        tar -zxvf helm-v3.6.0-linux-amd64.tar.gz

        sudo mv linux-amd64/helm /usr/local/bin/helm

        helm'
    - name: Sieve CI config generate
      run: 'echo "{\"workload_conditional_wait_timeout\": 1000}" > sieve_config.json

        cat sieve_config.json'
    - name: Build Image - learn
      run: 'python3 build.py -c examples/zookeeper-operator/ -m learn -p -r $IMAGE_NAMESPACE '
    - name: Build Image - test
      run: 'python3 build.py -c examples/zookeeper-operator/ -m test -p -r $IMAGE_NAMESPACE '
    - name: Build Image - vanilla
      run: 'python3 build.py -c examples/zookeeper-operator/ -m vanilla -p -r $IMAGE_NAMESPACE '
    - name: Clean images
      if: always()
      run: docker image prune -a -f && docker builder prune -a -f && docker system df
